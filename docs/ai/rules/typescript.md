# TypeScript

| Regra | Detalhe |
|-------|---------|
| `strict` | Sem `any` em código novo |
| Status / flags | `as const`, não TS `enum` |
| Secrets | Env / painel — nunca no repo |
| Arquivo | Máx. **300 linhas** |
| Escopo | Diff mínimo; sem rename sem pedido; sem helpers extras sem pedido |
